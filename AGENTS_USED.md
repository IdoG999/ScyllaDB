# Agents / Prompts Used

This project was built with Cursor's coding agent in iterative steps:

1. Designed architecture for a lightweight pipeline (lead finder, personalizer, workflow, DB, report).
2. Implemented Python modules and CLI.
3. Added mocked input data and prompt template.
4. Ran a dry-run execution to generate a persistent report.

Primary prompt intent used during build:
- "Implement a GTM Hunter PoC that identifies DataStax-related leads, personalizes outreach with a mocked LLM, triggers dry-run sends, stores results in SQLite, and generates a report."
